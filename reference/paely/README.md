# Reference handling

The authoritative private integration references are maintained outside this repository workspace in the supplied reference bundle. They are read-only inputs and are not republished with public artifacts. Compatibility decisions are recorded in `docs/PAELY_PRIVATE_COMPATIBILITY.md`.
